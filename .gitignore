# Build output
node_modules
dist
*.tsbuildinfo

# Runtime data
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment
.env
.env.*

# Editor / IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# ── Local tooling state (never commit) ─────────────────────────────
# Per-user Claude Code state lives outside the repo but can leak in
# when developers run Claude inside the repo directory.
.claude/
.agents/
.omc/
.codex/

# Temporary probe artifacts from debugging the Codex CLI hook bug
.codex-hook-probe.log
.codex-hook-*.stdin.json
codex-hook-probe/
codex-monorepo-local/

# Author-private design docs (keep in git only once finalized)
bbuddy-design-spec-v2.md

# Scratch files used during one-off migrations
.scratch-*
