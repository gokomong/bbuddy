# /bbddy:off — 컴패니언 음소거

사용자가 `/bbddy:off` 를 입력하면 이 스킬을 실행한다.

## 역할

`bbddy_mute` MCP 도구를 호출해서 컴패니언의 자동 반응(observer/chime-in)을 끈다.
statusline 표시는 유지된다.

## 실행

```
bbddy_mute({})
```

결과 메시지를 간단히 표시한다.
